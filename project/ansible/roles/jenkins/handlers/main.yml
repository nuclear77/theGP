---
- name: restart jenkins
  systemd:
    name: jenkins
    state: restarted

- name: safe reload jenkins
  community.general.jenkins_script:
    script: "Jenkins.instance.doReload()"
    url: http://localhost:8080
    wait: yes
    timeout: 60
